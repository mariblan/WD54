// You can work here or download the template!
// 1. Initialize an Array of Numbers:
// 1.1. Create an array called numberArray containing the following values in order: 10, 20, 30, 40, 50.
 let numberArray = [10, 20, 30, 40, 50];

// 2. Iterate Over the Array with a for Loop:
// 2.1. Use a for loop to iterate over numberArray and print each value to the console.
for (let i = 0; i < numberArray.length; i++){
    console.log(numberArray[i]);
}

// 3. Iterate Over the Array with a for...of Loop:
// 3.1. Use a for...of loop to iterate over numberArray and print each value to the console.
for (let value of numberArray){
    console.log(value);
}